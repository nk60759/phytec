/*to print the greatest no */
#include<stdio.h>
int main()
{
    int a,b;
    scanf("%d",&a);
    scanf("%d",&b);
   if (a>b){
       printf("the greatest value is:%d",a);
   }
    else {
       printf("the greatest value is b:%d",b);
   }
    return 0;
}