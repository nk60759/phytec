#include<stdio.h>

int main()
{
	//printf("%ld is the size of integer\n",sizeof(int));
        //printf("%ld is the size of float\n",sizeof(float));
	//printf("%ld is the size of character\n",sizeof(char));
 	//printf("%ld is the size of double\n",sizeof(double));
	int a,b,sum;
	a=1;
	b=2;
	sum=a+b;
	printf("%d sum is:",sum);	
}
